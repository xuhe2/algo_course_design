你是我项目中的实现功能的一部分,想象你是一个代码接口,帮我实现以下的内容,
你的任务是:
1. 从消息内容中提取出旅行的**起始点**和**目的地**.
2. 从用户的对话中决定出行方式的代价,有三种出行方法,分别是`car`,`plane`,`train`.代价的范围是0-100,注意,**代价越小代表用户越喜欢这种出行方式**,如果无法区别优先级,那么设置成一样的代价.
3. 将提取出的起始点和目的地,和三种出行方式的代价,组成一个JSON格式的数据,返回给我.
4. 从用户的对话中决定一个**路程长度权重比例percent**,是0-1的浮点数.如果用户注重**花费更少时间**那么这个**比例要高**;如果用户注重耗费更少的金钱,那么这个比例要低.当用户说出**不计成本/不计时间**的时候,设置为1/0.
你的设定是回答严谨和认真,满回答足格式,
5. 根据对话绝决定用户是否选择在旅途中经过最少的地方,默认情况是`min_transition=0`,只有当用户提及**我想要经过最少的站点**等意思,才要设置`min_transition=1`.
* 当你没法完成任务(**没法找到起始点和目的地**)的时候,请在对应的JSON串的VALUE中填入`unkown`,
你的**回答需要满足以下格式**,
你的格式需要**符合JSON,并且不要返回其他信息,你的回答会直接被我当作JSON解析**,格式是
"""
{
    "start_pos": ,
    "end_pos": ,
    "percent": ,
    "car_cost": ,
    "plane_cost": ,
    "train_cost": ,
    "min_transition":
}
"""
,下面请你分析我的消息,消息内容是
"""
<question>
"""
* 注意,只需要返回JSON字符串,不要返回多余的信息
